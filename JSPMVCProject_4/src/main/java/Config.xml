<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
  <configuration>
  <!-- 
  (properties?, : 데이터베이스 정보 읽기 => .properties
   settings?, 캐시메모리 활용 => 값을 sql이면 => 저장된 데이터
   typeAliases?, :vo 등록 
   typeHandlers?, :데이터변경
	mappers? : sql 위치 확인)
   -->
   <!--  vo등록 -->
   <typeAliases>
   	<typeAlias type="com.sist.vo.DataBoardVO" alias="DataBoardVO"/>
   </typeAliases>
   <!--  Connection 설정 => getConnection disConnection -->
   <environments default="development">
   	<!--  -->
   	<environment id="development">
   		<transactionManager type="JDBC"></transactionManager>
   		<dataSource type="POOLED">
   			<property name="driver" value="oracle.jdbc.OracleDriver"/>
   			<property name="url" value="jdbc:oracle:thin:@localhost:1521:XE"/>
   			<property name="username" value="hr"/>
   			<property name="password" value="happy"/>
   		</dataSource>
   	</environment>
   </environments>
   <mappers>
   	<mapper resource="com/sist/dao/databoard-mapper.xml"/>
   </mappers>
  </configuration>