<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
  <configuration>
  <!-- VO 등록 -->
  <typeAliases>
	<typeAlias type="com.sist.dao.ZipcodeVO" alias="ZipcodeVO"/>
  </typeAliases>
  <!-- 오라클 환경설정 : 정보등록 : DBCP -->
  <environments default="development">
  	<environment id="development">
  		<transactionManager type="JDBC"/>	<!-- autoCommit -->
  		<dataSource type="POOLED">	<!-- ConnectionPool -->
  			<property name="driver" value="oracle.jdbc.driver.OracleDriver"/>
  			<property name="url" value="jdbc:oracle:thin:@localhost:1521:XE"/>
  			<property name="username" value="hr"/>
  			<property name="password" value="happy"/>
  			<!-- 
  			Conneciton POOL : 미리 커넥션 연결
  			몇개 연결 / 몇개 사용 / 반환시까지 기다리는 시간
  			maxActive / maxIdle / maxWait
  			8			/	8		/	10000   (default)
  			 -->
  		</dataSource>
  	</environment>
  </environments>
  <!-- SQL문장 저장소 : Mapper 등록 -->
  <mappers>
	<mapper resource="com/sist/dao/post-mapper.xml" />
  </mappers>
  </configuration>